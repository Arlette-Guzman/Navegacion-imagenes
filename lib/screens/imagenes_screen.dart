import 'package:flutter/material.dart';

class ImagenesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Imagenes Screen"),
          centerTitle: true,
        ),
        body: Container());
  }
}
